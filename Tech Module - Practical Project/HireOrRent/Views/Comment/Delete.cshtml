@model HireOrRent.Models.Comment

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this comment?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Content)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Content)
        </dd>
        <dt></dt>
        <dd>

        <br />

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-group">
                @Html.ActionLink("Back to advertisement", "Details", "Advertisement", new { @id = Model.AdvertisementId }, new {@class = "btn btn-warning"})
                <input type="submit" value="Delete comment" class="btn btn-danger" />
            </div>
        }
        </dd>
    </dl>
</div>